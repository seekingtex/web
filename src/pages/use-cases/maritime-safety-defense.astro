---
import PageLayout from '~/layouts/PageLayout.astro';
import { articleSchema } from '~/lib/seo/schema';
import { nodes } from '~/content/graph/nodes';
import { getPageContent } from '~/i18n/page-content';

const lang = 'en';
const page = getPageContent(lang, 'maritime-safety-defense');
const node = nodes.find((n) => n.id === 'maritime-safety-defense');

const graphJsonLd = node
  ? {
      '@context': 'https://schema.org',
      '@graph': [
        articleSchema({
          headline: page.seo?.headline || 'Outdoor & Adventure Gear Fabrics — OEM Manufacturing',
          description: page.seo?.description || 'Seekingtex manufactures functional fabrics for outdoor and adventure gear: waterproof breathable shells, moisture-wicking base layers, durable shell fabrics.',
          datePublished: page.seo?.datePublished || '2024-06-01',
          dateModified: page.seo?.dateModified || '2026-06-01',
          author: 'Seekingtex',
          publisher: 'Seekingtex',
        }),
        {
          '@type': 'Product',
          name: node?.name?.en || 'Outdoor & Adventure Gear Fabrics',
          description: node?.description?.en || 'Functional fabrics engineered for outdoor apparel, adventure gear, and weather protection.',
          brand: { '@type': 'Brand', name: 'Seekingtex' },
          category: 'Outdoor & Adventure Fabrics',
        },
      ],
    }
  : null;

const metadata = {
  title: page.seo?.headline || 'Outdoor & Adventure Gear Fabrics — OEM Manufacturing',
  description: page.seo?.description || 'Seekingtex manufactures functional fabrics for outdoor and adventure gear: waterproof breathable shells, moisture-wicking base layers, durable shell fabrics.',
};
---

<PageLayout metadata={metadata}>
  <script slot="before" is:inline type="application/ld+json" set:html={graphJsonLd ? JSON.stringify(graphJsonLd) : ''} />

  <section class="mx-auto max-w-4xl px-6 py-16">
    <span class="text-sm font-medium text-sky-600 tracking-wide uppercase">{page.label || 'Use Case'}</span>
    <h1 class="font-semibold text-gray-900 dark:text-gray-100 mt-4 text-4xl">
      {page.headline || 'Engineering Excellence for Outdoor & Adventure Gear Fabrics'}
    </h1>
    <p class="text-lg text-gray-500 dark:text-gray-400 mt-3">
      {page.subtitle || 'Seekingtex delivers uncompromising quality for outdoor apparel — waterproof breathable shells, insulated mid-layers, and performance base layers.'}
    </p>

    <div class="mt-12 prose prose-gray dark:prose-invert max-w-none" set:html={page.body || ''} />
  </section>
</PageLayout>
