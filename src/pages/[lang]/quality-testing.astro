---
export const prerender = true;

import { languages } from '~/i18n/config';

export async function getStaticPaths() {
  return Object.keys(languages).map((lang) => ({ params: { lang } }));
}

return Astro.redirect(`/${Astro.params.lang}/quality`);
---
